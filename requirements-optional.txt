# ============================================================
# SKILLOR — OPTIONAL extras (NOT installed in CI by default)
# Install only if you need the feature:
#   pip install -r requirements-optional.txt
# ============================================================

# ---- Voice cloning (Chatterbox) ----
# Heavy GPU-oriented stack. GitHub-hosted runners are CPU-only — our own
# release notes warned Chatterbox can time out there, which is why
# production runs use TTS_ENGINE=kokoro. Use this only on a GPU runner or
# a self-hosted machine.
chatterbox-tts>=0.1.7
# Chatterbox needs this for its internal audio watermarker
# (self.watermarker = perth.PerthImplicitWatermarker()). If this package is
# missing or a name-clashing "perth" package gets installed instead,
# ChatterboxTTS.from_pretrained() fails with:
#   TypeError: 'NoneType' object is not callable
resemble-perth>=1.0.1
# Silences a FutureWarning from diffusers (used internally by Chatterbox):
# "LoRACompatibleLinear is deprecated... switch to PEFT backend".
peft>=0.10.0
torchaudio>=2.1.0
transformers>=4.40.0

# ---- Browser automation (screenshot fallback layer for scene visuals) ----
# Only needed if you set ENABLE_SCREENSHOT_FALLBACK=true. Remember to also
# run `playwright install chromium` — the pip package alone ships no browser.
playwright>=1.44.0
